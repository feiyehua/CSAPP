# System-Level I/O

## 10.10 Standard I/O
C programming language provides a higher-level alternative to Unic I/O, including `fopen`, `fclose` and `fwrite`.

They uses FILE as an abstraction for a file descriptor and a stream buffer.